"use client";
import {useMemo,useState} from "react";
type Output={year:number;type:string;title:string;authors:string;venue:string;url:string};
export default function Publications({initial}:{initial:Output[]}){const[filter,setFilter]=useState("All");const types=["All",...Array.from(new Set(initial.map(x=>x.type)))];const visible=useMemo(()=>filter==="All"?initial:initial.filter(x=>x.type===filter),[filter,initial]);return <div><div className="filters" role="group" aria-label="Filter research outputs">{types.map(type=><button key={type} className={filter===type?"active":""} onClick={()=>setFilter(type)}>{type}</button>)}</div><div className="pubList">{visible.map(item=><article key={item.title} className="pub"><div><span className="outputType">{item.type}</span><span className="year">{item.year}</span></div><h3><a href={item.url} target="_blank" rel="noreferrer">{item.title}</a></h3><p>{item.authors}</p><p className="venue">{item.venue}</p></article>)}</div></div>}
