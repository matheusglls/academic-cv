import{mkdir,writeFile}from"node:fs/promises";
const AUTHOR="A5058545919",ORCID="0000-0001-5375-2335",OSF_NODE="5k9yv",apiKey=process.env.OPENALEX_API_KEY;
const labels={article:"Journal article",review:"Review",preprint:"Preprint","conference-paper":"Conference paper","conference-abstract":"Conference abstract",dataset:"Dataset",software:"Software",book:"Book","book-chapter":"Book chapter"};
const works=[];let cursor="*";const select="id,title,publication_date,publication_year,type,authorships,primary_location,locations,doi,ids";
do{const url=new URL("https://api.openalex.org/works");url.searchParams.set("filter",`author.id:${AUTHOR}`);url.searchParams.set("per_page","200");url.searchParams.set("cursor",cursor);url.searchParams.set("select",select);if(apiKey)url.searchParams.set("api_key",apiKey);const r=await fetch(url);if(!r.ok){console.warn(`OpenAlex unavailable (${r.status})`);break}const j=await r.json();works.push(...j.results);cursor=j.meta?.next_cursor||""}while(cursor);
const normalized=works.map(w=>{const doi=(w.doi||"").toLowerCase();const protocol=doi.includes("10.17504/protocols.io");return{id:w.id,date:w.publication_date||`${w.publication_year}-01-01`,year:w.publication_year,type:protocol?"Protocol":labels[w.type]||"Other output",title:w.title||"Untitled",authors:(w.authorships||[]).map(a=>a.author?.display_name).filter(Boolean).join(", "),venue:w.primary_location?.source?.display_name||"",url:w.doi||w.primary_location?.landing_page_url||w.id,doi}});
async function crossrefByOrcid(){const out=[];let cursor="*";for(let page=0;page<10;page++){const u=new URL("https://api.crossref.org/works");u.searchParams.set("filter",`orcid:${ORCID}`);u.searchParams.set("rows","1000");u.searchParams.set("cursor",cursor);u.searchParams.set("mailto","matheusgallasl@gmail.com");const r=await fetch(u);if(!r.ok)break;const j=await r.json();for(const x of j.message?.items||[]){const doi=(x.DOI||"").toLowerCase();const protocol=doi.includes("10.17504/protocols.io");const crossType=x.type==="proceedings-article"?"Conference paper":x.type==="dataset"?"Data / code":x.type==="posted-content"?"Preprint":"Journal article";const parts=x.published?.["date-parts"]?.[0]||x.created?.["date-parts"]?.[0]||[];const date=[parts[0]||0,String(parts[1]||1).padStart(2,"0"),String(parts[2]||1).padStart(2,"0")].join("-");out.push({id:`crossref:${doi}`,date,year:parts[0],type:protocol?"Protocol":crossType,title:(x.title||[])[0]||"Untitled",authors:(x.author||[]).map(a=>[a.given,a.family].filter(Boolean).join(" ")).join(", "),venue:(x["container-title"]||[])[0]||"",url:doi?`https://doi.org/${doi}`:x.URL||"",doi})}const next=j.message?.["next-cursor"];if(!next||next===cursor)break;cursor=next}return out}
async function biorxiv(){const out=[];const r=await fetch(`https://api.biorxiv.org/details/orcid/${ORCID}/0`);if(!r.ok)return out;const j=await r.json();for(const x of j.collection||[]){const doi=(x.doi||"").toLowerCase();out.push({id:`biorxiv:${doi}`,date:x.date,year:Number((x.date||"").slice(0,4)),type:"Preprint",title:x.title,authors:x.authors||"",venue:x.server||"bioRxiv",url:`https://doi.org/${doi}`,doi})}return out}
async function osfRegistrations(node){const out=[];let next=`https://api.osf.io/v2/nodes/${node}/registrations/?page[size]=100`;while(next){const r=await fetch(next);if(!r.ok)break;const j=await r.json();for(const x of j.data||[])out.push({id:`osf:${x.id}`,date:x.attributes?.date_registered||x.attributes?.date_created,year:Number((x.attributes?.date_registered||x.attributes?.date_created||"").slice(0,4)),type:"Registration",title:x.attributes?.title||"OSF registration",authors:"Matheus Gallas Lopes and collaborators",venue:"Open Science Framework",url:`https://osf.io/${x.id}/`,doi:""});next=j.links?.next||""}return out}
normalized.push(...await crossrefByOrcid(),...await biorxiv(),...await osfRegistrations(OSF_NODE));
const clean=s=>String(s||"").replace(/<[^>]*>/g," ").replace(/\s+/g," ").trim();
const seen=new Set(),deduped=normalized.map(x=>({...x,title:clean(x.title),authors:clean(x.authors),type:x.type==="Review"?"Journal article":x.type})).filter(x=>{if(!x.title)return false;const titleKey=x.title.toLowerCase().replace(/[^a-z0-9]+/g," ").trim();const key=`${x.type}:${titleKey}`;if(seen.has(key))return false;seen.add(key);return true}).sort((a,b)=>(b.date||"").localeCompare(a.date||""));
if(!deduped.length)throw new Error("No outputs were retrieved; refusing to overwrite the current data file.");
await mkdir("data",{recursive:true});await writeFile("data/publications.json",JSON.stringify({generated_at:new Date().toISOString(),outputs:deduped},null,2)+"\n");console.log(`Wrote ${deduped.length} unique outputs`);
const people=new Map(),countries=new Map();for(const w of works){for(const a of w.authorships||[]){const name=a.author?.display_name;if(name&&!/matheus gallas/i.test(name))people.set(name,(people.get(name)||0)+1);for(const inst of a.institutions||[]){if(inst.country_code)countries.set(inst.country_code,(countries.get(inst.country_code)||0)+1)}}}const collaborations={generated_at:new Date().toISOString(),coauthors:[...people].sort((a,b)=>b[1]-a[1]).slice(0,20).map(([name,works])=>({name,works})),countries:[...countries].sort((a,b)=>b[1]-a[1]).map(([code,works])=>({code,works}))};await writeFile("data/collaborations.json",JSON.stringify(collaborations,null,2)+"\n");
